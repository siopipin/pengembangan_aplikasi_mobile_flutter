import 'package:flutter/material.dart';

class NewsCommentScreen extends StatelessWidget {
  const NewsCommentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Komentar 1"),
    );
  }
}
